
Instructions on Converting ASN.1 to XDDL 

- Download the latest 36.331 spec
- Save it as text
- Update variables in Makefile
- run asn-strip
- edit $(ASNFILE) by hand.  Some of the lines may have been broken at '-'
  characters.  Just run xo $(ASNFILE) repeatedly until there are no errors.
- Add $(ASNFILE) to git once it is correct.

